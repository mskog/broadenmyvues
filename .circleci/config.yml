version: 2.1
orbs:
  cypress: cypress-io/cypress@1

workflows:
  build:
    jobs:
      - build:
        name: "build"
        build: "npm run build"
      - cypress/run:
        requires:
          "build"
